// export const myGetter = (state) => {
//     return state.any
// };

export const currentStatus = (state) => {
  return state.status;
};

export const currentUser = (state) => {
  return state.user;
};
